{
  "check_reconciliation": ["do two sources agree on this figure?", "cross-check the published counts between sources", "which datasets disagree across sources?"],
  "find_anomalies": ["which datasets showed an unusual update anomaly?", "flag datasets with odd record-count jumps", "what anomalies did the latest detection find?"],
  "find_by_licence": ["what can I reuse under an open licence?", "list datasets licensed for commercial use", "which datasets share this licence?"],
  "find_deteriorating": ["which datasets are trending worse over time?", "show datasets whose freshness is deteriorating", "what has been getting less reliable lately?"],
  "find_recovering": ["which datasets have started updating again?", "show datasets recovering from a stale period", "what is improving in the catalogue?"],
  "find_schema_drift": ["which datasets changed their structure?", "detect column or schema changes in the catalogue", "has any dataset gained or lost fields recently?"],
  "find_stale": ["which datasets are stale or aging right now?", "find datasets at risk of freshness problems", "list degraded datasets"],
  "find_unreliable": ["which datasets fail to publish reliably?", "list datasets with a poor publishing record", "what is the least reliable source in the catalogue?"],
  "get_data_passport": ["produce the dataset passport for this dataset", "give me the machine-readable passport v1 for this dataset", "what does the passport say about this dataset's evidence?"],
  "get_dataset": ["what is the status of this dataset?", "show me the details for one dataset id", "is this dataset still updating?"],
  "get_evidence": ["show me the evidence receipt for this dataset's last probe", "audit the published evidence for this dataset", "what did the last observation record for this dataset?"],
  "get_freshness_summary": ["how fresh is the catalogue overall?", "summary of stale versus fresh datasets", "which sources are lagging across the catalogue?"],
  "get_provenance": ["can I cite this source?", "what licence applies and how should I attribute it?", "where did this dataset's numbers come from?"],
  "search_datasets": ["find datasets about road accidents", "which Malaysian datasets cover household income?", "list open datasets published by a given agency"],
  "trust_verdict": ["give me an overall trust verdict for this dataset", "should I warn users before they rely on this data?", "summarise the published trust signals"],
  "usage_summary": ["how many tool calls were made this week?", "which DataPulse tools are used most?", "summarise aggregate anonymous usage over the last month"],
  "verify_attestation": ["check the signed attestation chain for this dataset", "is the published attestation signature valid?", "verify the day's signed envelope"],
  "verify_dataset": ["can I rely on this dataset today?", "before I use this dataset, has it changed recently?", "check whether this dataset is fit to rely on"],
  "verify_evidence": ["compare the published evidence against a fresh live check", "is the published freshness claim still true right now?", "verify the transport evidence for this dataset live"]
}
