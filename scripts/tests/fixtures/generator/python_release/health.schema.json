{
  "properties": {
    "datasets": {
      "items": {
        "properties": {
          "status": {"enum": ["fresh", "aging", "stale", "discontinued", "degraded", "browser-dependent", "unreachable", "unknown", "unknown-freshness", "reference"]}
        }
      }
    }
  }
}
