<!doctype html>
<!-- generated: scripts/gen_landing_page.py; inputs: config/landing-page.json, config/public-surfaces.json, scripts/templates/landing.html.tmpl; do not hand-edit -->
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="{{description}}">
  <title>{{title}}</title>
  <link rel="canonical" href="{{canonical_href}}">
  <meta http-equiv="refresh" content="0; url={{canonical_href}}">
</head>
<body>
  <p>The <a href="{{canonical_href}}">DataPulse dataset register</a> has moved to the canonical root route.</p>
</body>
</html>
