<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="color-scheme" content="light dark">
  <title>$title$ | DataPulse MY</title>
  <style>
    :root { color-scheme: light dark; font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; line-height: 1.6; }
    body { margin: 0; background: #f6f8fb; color: #182230; }
    main { box-sizing: border-box; max-width: 52rem; margin: 2rem auto; padding: clamp(1.25rem, 4vw, 3rem); background: #fff; border-radius: .5rem; box-shadow: 0 .25rem 1rem #18223018; }
    h1, h2, h3 { line-height: 1.25; color: #102a43; }
    h1 { margin-top: 0; }
    a { color: #075985; }
    code { padding: .1em .3em; border-radius: .2em; background: #e8edf3; }
    pre { overflow-x: auto; padding: 1rem; border-radius: .3rem; background: #e8edf3; }
    table { display: block; overflow-x: auto; border-collapse: collapse; }
    th, td { padding: .5rem .75rem; border: 1px solid #cbd5e1; text-align: left; }
    @media (prefers-color-scheme: dark) {
      body { background: #111827; color: #e5e7eb; }
      main { background: #1f2937; box-shadow: none; }
      h1, h2, h3 { color: #f8fafc; }
      a { color: #7dd3fc; }
      code, pre { background: #111827; }
      th, td { border-color: #475569; }
    }
  </style>
</head>
<body>
  <!-- Generated by scripts/gen_health_methodology_html.py from docs/health-methodology.md; do not edit. -->
  <main>
$body$
  </main>
</body>
</html>
