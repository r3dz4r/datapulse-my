-r requirements.txt
-r mcp/requirements.txt

jsonschema==4.25.0
pytest==8.4.1
# requests is used by tests/test_notebook_examples.py (monkeypatches requests.get)
requests==2.32.5
# pandas is used by the trust-layer-notebook example (cell-2 loads JSON into a DataFrame)
pandas==2.2.3
