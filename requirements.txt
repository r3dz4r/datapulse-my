# Provides google.transit.gtfs_realtime_pb2.FeedMessage.
gtfs-realtime-bindings==2.2.0
httpx==0.28.1
# pyarrow / parquet read/write support
pyarrow>=15.0.0,<20.0.0
# PDF date extraction (for ST National Energy Balance, etc.)
pdfplumber>=0.10,<1.0
