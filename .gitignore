__pycache__/
.venv/
node_modules/
.cache/datapulse/metrics_dataset_cumul.json
.DS_Store
codeburn*.json

# Tailscale certs from earlier Funnel experiment (root-owned, not for repo)
vps.tail*.ts.net.crt
vps.tail*.ts.net.key

# Runtime state (operational, not source)
var/

# Codex worker planning scratch
tasks/
