__pycache__/
.venv/
node_modules/
.cache/datapulse/metrics_dataset_cumul.json
.DS_Store
codeburn*.json

# Tailscale certs from earlier Funnel experiment (root-owned, not for repo)
vps.tail*.ts.net.crt
vps.tail*.ts.net.key

# Runtime state (operational, not source)
var/

# Codex worker planning scratch
tasks/
.codex_output/

# Record-evidence daily artifacts (regenerated by pipeline; latest.json is canonical)
record-evidence/**/2*.json
record-evidence/**/2026-*.json
.dotfiles/

# Secrets — never commit real keys (Paddle, API, tokens)
.env
.env.*
!.env.example

# Health observation history (rolling 7-day window, regenerated every probe cycle).
# Older rows live in ~/runtime/datapulse-history/health-YYYY-MM.jsonl.gz
health/history.jsonl
docs/.health-methodology/
