{
  "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": true,
    "readOnlyHint": true
  },
  "capabilities": [
    "find_anomalies",
    "find_anomalies.limit",
    "find_anomalies.min_reliability",
    "find_anomalies.mode"
  ],
  "contract_version": "1.0.0",
  "dataset_count": 418,
  "description": "Return datasets flagged by the latest published anomaly detection (anomalies), ranked by how far the observed update interval exceeds its threshold. Optionally require a minimum publish-reliability grade; includes pipeline-computed anomaly and reliability evidence so agents do not recompute it. Use it for unusual update intervals; do not use it for worsening freshness, recovery, reliability grades, or structural drift—use find_deteriorating, find_recovering, find_unreliable, or find_schema_drift instead. It reads precomputed anomaly data, so an empty result means no published row survives the selected filters; DataPulse is read-only, requires no API key, and the edge limits clients to roughly one request per second with a small burst, so pace or retry.",
  "displayName": "find_anomalies",
  "endpoint": {
    "method": "POST",
    "transport": "streamable-http",
    "url": "https://mcp.data-pulse.my/mcp"
  },
  "identifier": "urn:air:data-pulse.my:mcp:find_anomalies",
  "input_schema": {
    "additionalProperties": false,
    "properties": {
      "limit": {
        "default": 50,
        "description": "Maximum highest-ranked anomalies to return, e.g. 50; omit it to use 50 without changing the ranking.",
        "examples": [
          10,
          50
        ],
        "maximum": 200,
        "minimum": 1,
        "type": "integer"
      },
      "min_reliability": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Optional inclusive published reliability floor, e.g. 'C' keeps A, B, and C; omit it to retain rows regardless of grade.",
        "examples": [
          "A",
          "C"
        ]
      },
      "mode": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Optional exact published detection mode filter, e.g. 'rolling_14d' or 'cadence_fallback'; omit it to include every mode.",
        "examples": [
          "rolling_14d",
          "cadence_fallback"
        ]
      }
    },
    "required": [],
    "type": "object"
  },
  "source": {
    "commit_sha": "aa43e74dc0c721adcec420b50da7a5587180ef91",
    "manifest": "mcp.json"
  },
  "specVersion": "1.0",
  "tags": [
    "anomalies",
    "freshness",
    "malaysia",
    "read-only",
    "trust-layer"
  ],
  "tool_type": "mcp-tool",
  "verification": {
    "model": "Signed probe attestation (Ed25519 L1, git-tag L2 anchor, per-dataset cosign keyless Sigstore bundle)",
    "verification_command": "cosign verify-blob --bundle <dataset>.sigstore.json --certificate-identity <oidc-identity> --certificate-oidc-issuer https://token.actions.githubusercontent.com",
    "verification_metadata_url": "https://www.data-pulse.my/llms.txt"
  }
}
