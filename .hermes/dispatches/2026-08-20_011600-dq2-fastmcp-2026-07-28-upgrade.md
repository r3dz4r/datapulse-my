Workdir: /home/redza/datapulse-my (git repo, branch=main, working tree clean at dispatch time per `git status`).

Goal: Upgrade the datapulse MCP server to the MCP `2026-07-28` spec, lifting M8ven grade D→C (~70/100) and adding the modern spec features (stateless mode, `ttlMs`+`cacheScope` on list responses, `_meta`-based identity, `listChanged` notifications) in a single dispatch. Concurrently fill the 11 missing tool annotation sets per `mcp/AGENTS.md` rule 7.

Failure mode: A malimplemented spec upgrade breaks Glama/mcp.so/MCPGrade indexing (the M8ven gating check is `inspect {protocolVersion:"2026-07-28"}.initialize` handshake returns 200 with `serverInfo`), and the 11 unannotated tools continue to fail OpenAI's directory rejection path. We waste 1-2 dispatch days and either ship a partial upgrade or have to roll back.

Acceptance test: (1) `git status` clean before dispatch; (2) pin bump in `mcp/requirements.txt`: `fastmcp>=4.0.0,<5` (verify 4.x supports `Stateless = true` + `ttlMs`/`cacheScope` annotations BEFORE pinning — if 4.x doesn't have these yet, pin to a beta branch or stop and surface); (3) `mcp/server.py` adds `Stateless=True` to the FastMCP() constructor call; (4) `tools/list` response carries `ttlMs: 300000` and `cacheScope: "private"` (5-min refresh window is correct for the static 16-tool catalogue); (5) every existing tool (currently: search_datasets, get_dataset, find_stale, get_provenance, find_by_licence, trust_verdict, verify_attestation, verify_evidence, find_anomalies, find_deteriorating, find_recovering, find_unreliable, find_schema_drift, check_reconciliation, usage_summary, get_evidence) declares a `ToolAnnotations(readOnlyHint, destructiveHint, idempotentHint, openWorldHint)` block per `mcp/AGENTS.md` rule 7 — that's 16 tools × 4 hints = 64 annotations, currently some are missing; (6) `tests/` adds ≥1 in-memory FastMCP client test per the new spec behaviour; (7) `uv run --with fastmcp,httpx pytest mcp/tests/ -v` passes 100%; (8) `mcp.json` regenerated via `python3 scripts/gen_mcp_reference.py` and validated `jq '.tools | length' mcp.json` returns 16 with all annotations present; (9) `git commit -m "feat(mcp): upgrade to 2026-07-28 spec + add 11 missing annotation sets"` and **DO NOT push** (operator reviews); (10) `git log -1 --format=%s` shows the commit message above; (11) operator verifies `curl https://mcp.data-pulse.my/mcp -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2026-07-28",...}}'` returns 200 with `serverInfo`, then submits to M8ven + mcpgrade for re-grade.

Recommended execution model: terra — terra not luna because: (a) FastMCP 4.0 API surface may have new constructor args + behaviour that requires reading 4.0 release notes BEFORE applying (unknowns); (b) verifying that `Stateless=True` + `ttlMs`/`cacheScope` actually map to FastMCP 4.x kwargs (not just documented in spec); (c) the 11 tools missing annotations is a non-trivial diff (5-15K chars depending on existing structure); (d) `mcp.json` regeneration step has fragile output ordering; (e) test rewriting is mechanical but cross-references check_reconciliation/usage_summary which have non-obvious input schemas. Luna would burn budget on the FastMCP 4.0 read-then-write cycle. Sol would not bring leverage here — sol's value is in cross-cutting diagnosis or planning; this is implementation. If FastMCP 4.0 is NOT on PyPI yet (verify `pip index versions fastmcp` first), STOP and surface — operator decides on beta branch vs postpone.
