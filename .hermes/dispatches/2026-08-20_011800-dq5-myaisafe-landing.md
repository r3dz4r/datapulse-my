Workdir: /home/redza/datapulse-my (git repo, branch=main, working tree clean at dispatch time per `git status`).

Goal: Turn the existing operator-authored alignment-memo + the existing `docs/myaisafe-contract-inventory.md` and `docs/contract-inventory.md` (16-row contract inventory, source: commit `ae28c2d`) into a public-facing landing page on datapulse-my. Single static HTML page on the existing `https://www.data-pulse.my` site, served via GitHub Pages just like the rest of `docs/`. Bounded: copy-edit + minor HTML scaffolding, no new infrastructure. No new paths required.

Failure mode: A generated doc (per `docs/AGENTS.md` rule) gets hand-edited and the deterministic-safety-net gate fails the next deploy. Or a `[skip deploy]`-prone file lands on main and the page never actually ships.

Acceptance test: (1) `git status` clean before dispatch; (2) read `docs/AGENTS.md` rule list — specifically Hard rules 3 ("No fabricated dataset IDs"), 6 ("`docs/index.html` and `docs/npra.html` are GENERATED, not hand-edited"), and 8 ("No marketing superlatives" — "category-defining", "industry-leading", "uncrowded" are retired framings per `docs/AGENTS.md`); (3) read existing `docs/myaisafe-contract-inventory.md` (operator source) and identify the 16-row contract table from commit `ae28c2d`; (4) confirm path: `docs/myaisafe.html` is NOT a generated file (per docs taxonomy table) — this is hand-authored, so direct write is OK, but `scripts/embed_dashboard_data.py` may NOT be used for this; (5) produce a single `docs/myaisafe.html` file using standard Jekyll layout (`layout: default`, `title: MY-AISafe Alignment`), that contains: (a) a heading section rephrasing the alignment-memo's claims as a public landing page (using verifiable numbers only); (b) the 16-row contract inventory table from `ae28c2d`; (c) a methodology note citing `docs/health-methodology.md` methodology_version (currently `3`); (d) a "Where this differs from the alignment-memo" footer pointing operator-curious readers back to the source notes; (6) **NEVER include**: "category-defining", "industry-leading", "first mover", "uncrowded", "trust us" framings; (7) add `docs/myaisafe.html` to the `paths:` list in `.github/workflows/deploy-pages.yml` (it's not currently there — the new file won't deploy otherwise); (8) verify all numbers are reproducible from `datapulse.json` + `health/latest.json` — run `python3 scripts/check.py` to confirm zero fabricated IDs; (9) **DO NOT touch `docs/index.html` or `docs/npra.html`** (per docs/AGENTS.md rule 6); (10) commit: `git add docs/myaisafe.html .github/workflows/deploy-pages.yml && git commit -m "feat(docs): MY-AISafe alignment landing page"`, **DO NOT push**; (11) `git log -1 --format=%s` shows the commit; (12) operator reviews + verifies via `curl https://www.data-pulse.my/myaisafe` returns 200 + page after the next deploy cycle.

Recommended execution model: luna — pure mechanical: known structure (single hand-authored HTML file + single-line edit to deploy-pages.yml paths list), low arithmetic risk, and the source content (commit `ae28c2d`'s table) is already verified. Apply doc/AGENTS.md rule 8 carefully (no marketing superlatives) — that's the only creative-judgment moment.
