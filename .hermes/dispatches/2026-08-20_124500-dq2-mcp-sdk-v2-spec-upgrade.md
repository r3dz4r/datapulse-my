Workdir: /home/redza/datapulse-my (git repo, branch=main, working tree should be clean except `.hermes/` dispatches dir untracked; verify with `git status` before any commit).
Goal: Migrate `mcp/server.py` from `fastmcp` (legacy v3.4.5) to the **official MCP Python SDK v2 (`mcp>=2,<3`)** stable release, achieving MCP `2026-07-28` protocol compliance with a single dispatch. Specifically: (a) replace `from fastmcp import FastMCP` with `from mcp.server.mcpserver import MCPServer`, all 14 `@mcp.tool`/`@mcp.resource` decorators preserved (only the class name + import path change at the decorator surface); (b) add `cache_hints` on the `MCPServer` constructor so list responses carry `ttlMs: 300_000` + `cacheScope: "private"` per the 2026-07-28 spec; (c) update `mcp/requirements.txt` to `mcp>=2,<3` (replaces `httpx` with `httpx2`, adds `mcp-types` exact-pinned by `mcp`, adds `opentelemetry-api>=1.28`, bumps `pydantic>=2.12` on Python<3.14); (d) update `mcp/tests/` to use `httpx2` types instead of `httpx` in SDK-touched test fixtures; (e) regenerate `mcp.json` via `python3 scripts/gen_mcp_reference.py` and re-verify tool count + annotation completeness (already 16/16 annotations in `mcp.json` pre-migration, do NOT regress).
Failure mode: A botched migration silently breaks the `mcp.data-pulse.my` endpoint for 2025-era clients (Glama/MCPGrade crawlers + agent traffic). Pre-2026/2026-07-28 dual-serving is automatic per `mcp` v2 SDK design — both protocol eras speak through one `MCPServer` instance — but only if the migration completes cleanly. If we leave `fastmcp` and `mcp` both in `requirements.txt` we ship a confusing dual-dep project and registry graders flag it. If we apply the rename without verifying that `@mcp.tool` still works the decorator API we lose all 5 function tools and ship a server returning empty `tools/list`. If we forget to remove `httpx` from `requirements.txt` we ship a project that resolves to wrong `httpx` vs `httpx2` types silently — `except httpx.ConnectError:` blocks keep importing fine but stop matching. We waste 2 dispatch days and either ship a broken endpoint or have to roll back.
Acceptance test: (1) `git status` clean except `.hermes/` untracked before dispatch starts; `pip index versions mcp` confirms 2.0.0 latest stable (verified 2026-08-20 12:36 MYT); (2) `mcp/requirements.txt` changes only: `fastmcp==3.4.5` removed; `mcp>=2,<3` added; `httpx` removed (verify with `grep -rn "^[import ]*httpx[ \\.\\b]" mcp/` before removal — if test fixtures import `httpx.ConnectError` or `httpx.MockTransport`, switch to `httpx2` equivalents); (3) `mcp/server.py` changes: line 25 `from fastmcp import FastMCP` → `from mcp.server.mcpserver import MCPServer`; line 327 `mcp = FastMCP(...)` → `mcp = MCPServer("DataPulse MY", cache_hints={...})` preserving all existing kwargs EXCEPT `host`/`port` if any — recall v2 moved `host`/`port` OFF `MCPServer.__init__` onto `run()` / `streamable_http_app()`; preserve the bind behaviour with `mcp.run(host="0.0.0.0", port=...)` or `mcp.streamable_http_app()` per the migration guide's Transport section; (4) `CacheHint` imported from `mcp.server.mcpserver`, applied to the 6 cacheable methods where it makes sense (`tools/list`, `prompts/list`, `resources/list`, `resources/templates/list`, `resources/read`, `server/discover`) per the typed `Mapping[CacheableMethod, CacheHint]` API; static 16-tool + 9-resource catalogue — emit `ttlMs: 300000` + `cacheScope: "private"` per spec — 5min refresh window is correct for static catalogue; (5) all 5 `@mcp.tool` decorators + all 9 `@mcp.resource` decorators unchanged in count + name + behaviour; (6) `mcp/tests/` updated: any `httpx.ConnectError`/`pytest.raises(httpx...)` → `httpx2.ConnectError`; `httpx.MockTransport` → `httpx2.MockTransport`; (7) `uv run --with mcp,httpx2 pytest mcp/tests/ -v` runs 100% green, ≥1 in-memory `MCPServer` client test per new spec behaviour; (8) `mcp.json` regenerated via `python3 scripts/gen_mcp_reference.py`, validated `jq '.tools | length' mcp.json` returns 16 AND `jq '[.tools[] | select(.annotations == null)] | length' mcp.json` returns 0 (annotation completeness — was already 0 absent pre-migration, do NOT regress); (9) `git commit -m "feat(mcp): migrate fastmcp->mcp SDK v2 for 2026-07-28 spec compliance"` and DO NOT push (operator reviews); (10) `git log -1 --format=%s` shows that commit message verbatim; (11) operator verifies locally with the existing uvicorn/streamable-http invocation and re-grades via M8ven + mcpgrade + `curl -X POST https://mcp.data-pulse.my/mcp -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2026-07-28",...}}'` returns 200 with `serverInfo`.
Recommended execution model: luna