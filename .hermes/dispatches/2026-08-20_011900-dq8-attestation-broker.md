Workdir: /home/redza/datapulse-my (git repo, branch=main, working tree clean at dispatch time per `git status`). **Note: this is a TWO-PART delivery — operator writes the spec doc, codex writes the implementation brief only after operator lands the spec.** This dispatch covers BOTH parts sequenced.

Goal: Reposition the existing Agent Trust Protocol spec mapping from "stand-alone standard-setting" to a **4-format attestation broker** — datapulse's daily-head envelope becomes interoperable with (1) ATSA Server Attestation Document format, (2) OpenA2A ATX credential format, (3) in-toto Statement v1 predicate format, (4) OSCAL Assessment Results. Per novelty audit (2026-08-19) and second-opinion comparison, the cryptographic primitives are NOT novel; the broker is a **regional deployment** of established supply-chain formats — value comes from operating credibly, not from being first.

Failure mode: If the spec doc is wrong (e.g. mis-cites ATSA's SAD schema, or invents a custom ATX claim), codex implements against the wrong target and we ship a non-conformant broker. If codex ships without the spec doc, the `verify_attestation` extension returns bad data and downstream compliance reports fail.

Acceptance test (operator part — spec doc): (1) `git status` clean before dispatch; (2) `docs/mcp-attestation-spec.md` is a NEW hand-authored markdown file (NOT generated, NOT in the auto-deploy path per docs/AGENTS.md); (3) the spec maps each of these 4 formats to `datapulse/v1/daily-chain-head-envelope`:
   - **ATSA SAD**: cite the ATSA spec precisely — fields must match: `version`, `operator`, `key_id`, `clearance`, `tools[]`, `signature`. Highlight the bridge: ATSA handles server-admission, datapulse handles response-integrity — they're orthogonal trust layers. A datapulse server with a valid ATSA SAD has BOTH trust claims.
   - **OpenA2A ATX credential**: this is the 2 stars on GitHub repo (per novelty audit). Honestly note that ATX has zero adoption signal — the broker is FORWARD-COMPATIBLE, not "competing standard." ATSA has the same low-adoption risk.
   - **in-toto Statement v1 predicate**: this is the well-trodden path. Standard predicate types include `https://in-toto.io/attestation/release/v0.1` and `https://in-toto.io/attestation/provenance/v1`. The broker produces in-toto-compliant statements where the predicate body's `dataset_count`, `key_id`, `chain_head` fields are lifted from `chain_head.json`.
   - **OSCAL Assessment Results**: this is the regulator-evidence format (NIST). Bridge to NIST SP 800-53 AC-3/AC-4/AC-6 (least-privilege, info-flow, audit) and AU-2/AU-9 (audit record). This is the actual MOVE that turns the attestation chain from "developer toy" into "regulator-defensible" — write this section with concrete NIST cite-able claims.
   (4) spec doc concludes with: kill condition = "if ATSA merges into MCP spec by 2027-Q2, deprecate the ATSA bridge entry in `verify_attestation` and rely on the spec-blessed format". The spec is a draft — note "DRAFT — not a standards claim, not a reference implementation guide".
Acceptance test (codex part — implementation, dispatched AFTER operator lands the spec): (5) `mcp/server.py` extends `verify_attestation` to return 4 keyed sub-dicts: `{"atsa": {...}, "atx": {...}, "in_toto": {...}, "oscal": {...}}` (or 4 separate `verify_attestation_atsa`, `verify_attestation_atx`, etc. — operator decides naming based on FastMCP naming constraints); (6) `mcp/tests/` adds ≥4 tests, one per format, exercising the round-trip conversion; (7) `mcp.json` regenerated with the new tool(s); (8) `uv run --with fastmcp,httpx pytest mcp/tests/ -v` passes 100%; (9) commit: `feat(mcp): 4-format attestation broker — ATSA/ATX/in-toto/OSCAL`; (10) **DO NOT push**; (11) `git log -1 --format=%s` confirms.

Recommended execution model: **operator-led for the spec doc (1 hour, you write `docs/mcp-attestation-spec.md` first)**, then **terra for the implementation dispatch (after the spec is committed to the branch, codex-implemented via a separate dispatch invocation referencing `docs/mcp-attestation-spec.md` as the canonical source)**. Sol-tier audit before operator-led spec work is needed IF operator wants validation on the draft before committing — operator: if unsure on any of the 4 formats, dispatch `gpt-5.6-sol` with a brief like "audit my draft ATSA ↔ datapulse mapping; identify mis-citations or schema drift; return verdict per format (correct/near-correct/needs-revision)"; budget: 0.05 dispatch day.
